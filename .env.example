# Environment
NODE_ENV=development

# Server
PORT=5000

# Frontend URL (CORS)
UI_URL=http://localhost:3000

# JWT Configuration
ACCESS_TOKEN_SECRET=your_super_secret_jwt_key_change_this_in_production_min_32_chars

# Rate Limiting
LIMITER_TIME=900000
LIMITER_MAX_REQUESTS=100

# Database Configuration (Option 1: Individual params)
DB_HOST=localhost
DB_USER=postgres
DB_PORT=5432
DB_PASSWORD=your_database_password
DB_DATABASE=your_database_name

# Database Configuration (Option 2: Connection URL - takes precedence if set)
# DATABASE_URL=postgresql://user:password@localhost:5432/database_name
